#include <stdio.h>
int main()
{int l, b, p;
    printf("Enter the Length = ");
    scanf("%d",&l);
    printf("Enter the Breadth = ");
    scanf("%d",&b);
    p=2*(l+b);
    printf("The Perimeter of Rectangle is: %dcm",p);
return 0;
}
